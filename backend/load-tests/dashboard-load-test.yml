config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
      x-tenant-id: 'default'
scenarios:
  - flow:
    - get:
        url: '/dashboards'
