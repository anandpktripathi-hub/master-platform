// Keep import paths stable across the codebase.
// Use the canonical RolesGuard implementation (role normalization, @Public awareness).
export { ROLES_KEY, RolesGuard } from '../../guards/roles.guard';
